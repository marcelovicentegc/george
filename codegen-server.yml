overwrite: true
schema: "http://127.0.0.1:4000/api"
documents: ./src/gql/tags/*.graphql.{ts, tsx}
generates:
  ./src/server/@types/gql/index.ts:
    config:
      noNamespaces: true
    plugins:
      - typescript
      - typescript-resolvers
